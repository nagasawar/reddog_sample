<%@ tag pageEncoding="utf-8"%>

<%--
 *   markRquired.tag
 *     必須文字を定義する。一括で必須文字を管理したいために作成した。
 *     「*」以外の文字を指定したい場合は、タグ内を書き換える。
 *
 *     例) 「(必須)」にしたい場合。
 *       <span class="mark-required"> (必須)</span>
 --%>
<span class="mark-required"> *</span>